---
Name: grid-dev
Only:
  environment: dev
---

# E2E fixtures are served by the wedevelopnl/silverstripe-e2e module (dev
# dependency): it registers the /dev/e2e-fixtures endpoint and relaxes
# Session.strict_user_agent_check for the Playwright browser contexts. This
# block only supplies the grid's own fixtures and domain behavior.
#
# SECURITY: the endpoint is intentionally unauthenticated (its /reset action is
# a data-wipe primitive). The dev-only gating that makes this safe lives in the
# module: WeDevelop\E2e\Fixtures\FixtureController gates on Director::isDev()
# (canInit() + a defence-in-depth 404), and its _config registers the route
# with `Only: environment: dev`. Read that class before exposing this anywhere.
WeDevelop\E2e\Fixtures\FixtureLoader:
  # Everything a run can leave behind, declared by class: reset() deletes every
  # record of these and their subclasses on both stages, however it was created.
  # That covers what no reachability rule could — a shared block the library
  # still holds but no page places, an element a spec created by driving the
  # CMS, a placement whose page a crashed run took with it.
  #
  # The trade: a listed class is disposable, so anything YOU authored in one is
  # gone after a fixture load. `task seed-fixture` runs against this same dev DB.
  # Files are deliberately absent — the image an `attach_image` post-action
  # uploads survives, rather than a reset emptying the whole asset store.
  purge_classes:
    - Page
    - WeDevelop\Grid\Model\GridElement
    - WeDevelop\Grid\Model\SharedBlock
  # Navigation only: the loader walks this list in order to pick the page a
  # fixture load returns, falling back to the first SiteTree the fixture made.
  # Both entries are harness-app page types (see .docker/app/src/), not module
  # classes — the grid ships no page type of its own.
  fixture_page_classes:
    - Page
    - App\MultiZonePage
  # Grid fixtures are authored top-down (page → section → row → column → leaf),
  # so every container's children are declared in the YAML. The module forces
  # these statics off only for the duration of each fixture write, so
  # GridElement::onAfterWrite() does not scaffold an extra Row/Column that would
  # duplicate the ones the fixture creates itself.
  config_overrides:
    WeDevelop\Grid\Model\Section:
      auto_scaffold: false
    WeDevelop\Grid\Model\Row:
      auto_scaffold: false
  fixtures:
    element-tree:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/ElementTree.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    empty-page: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/EmptyPage.yml'
    collapse-test: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CollapseTest.yml'
    drag-and-drop:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/DragAndDrop.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    shared-blocks:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/SharedBlocks.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_shared_a
        - action: publish_recursive
          class: Page
          identifier: e2e_shared_b
    multi-zone:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/MultiZone.yml'
      post_actions:
        - action: publish_recursive
          class: 'App\MultiZonePage'
          identifier: e2e_multi_zone_page
    content-elements:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/ContentElements.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    ghost-jump:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/GhostJump.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-section-drop:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossSectionDrop.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-section-drop-single:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossSectionDropSingle.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-section-drop-single-reverse:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossSectionDropSingleReverse.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-row-column-drop:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossRowColumnDrop.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-row-column-drop-single:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossRowColumnDropSingle.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-row-column-drop-single-reverse:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossRowColumnDropSingleReverse.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    full-width-column-drop:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/FullWidthColumnDrop.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-column-element-drop:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossColumnElementDrop.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-column-element-drop-single:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossColumnElementDropSingle.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    cross-column-element-drop-single-reverse:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/CrossColumnElementDropSingleReverse.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    archive-test:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/ArchiveTest.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    media-elements:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/MediaElements.yml'
      post_actions:
        - action: attach_image
          class: 'WeDevelop\Grid\Model\ContentElement'
          identifier: media_element
          fields:
            relation: MediaImage
            source: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/assets/test-image.png'
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    duplicate-test:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/DuplicateTest.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_source_page
        - action: publish_recursive
          class: 'App\MultiZonePage'
          identifier: e2e_target_page
    complex-page:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/ComplexPage.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
        - action: unpublish
          class: 'WeDevelop\Grid\Model\ContentElement'
          identifier: draft_leaf
        - action: modify
          class: 'WeDevelop\Grid\Model\ContentElement'
          identifier: modified_leaf
          fields:
            Title: 'Modified Text Block (draft)'
        - action: modify
          class: 'WeDevelop\Grid\Model\Row'
          identifier: row1
          fields:
            Title: 'First Row (modified)'
        - action: unpublish
          class: 'WeDevelop\Grid\Model\Section'
          identifier: section2
    validation-errors:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/ValidationErrors.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    viewport-cascade:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/ViewportCascade.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    mobile-column-width:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/MobileColumnWidth.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
    # Not used by any spec — backs `npm run docs:screenshots`, so the images in
    # docs/images/ can be regenerated from a known page whenever the UI changes.
    docs-page:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/DocsPage.yml'
      post_actions:
        - action: publish_recursive
          class: Page
          identifier: e2e_page
        - action: unpublish
          class: 'WeDevelop\Grid\Model\ContentElement'
          identifier: opening_hours_block
        - action: modify
          class: 'WeDevelop\Grid\Model\ContentElement'
          identifier: office_block
          fields:
            Title: 'Office and parking'
    history-view-test:
      path: 'wedevelopnl/silverstripe-grid:tests/E2E/Fixture/HistoryViewTest.yml'
      # No post_actions: the history-view E2E spec creates all versions
      # via user actions (publish, edit, publish again) so each version
      # in the resulting history is the validated result of a real
      # interaction, not a fixture side-effect.
