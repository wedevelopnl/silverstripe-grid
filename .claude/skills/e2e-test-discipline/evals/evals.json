{
  "skill_name": "e2e-test-discipline",
  "evals": [
    {
      "id": 1,
      "prompt": "Add a 'Duplicate Row' button to the row actions toolbar that clones the row and all its columns",
      "expected_output": "Implementation includes E2E spec written as a user journey with test.step(), existing specs read before writing, playwright test commands attempted",
      "files": [],
      "expectations": [
        "Agent reads existing E2E specs in tests/E2E/specs/ before writing any new spec",
        "Agent creates or extends an E2E spec file in tests/E2E/specs/",
        "E2E spec uses test.step() for individual operations within a test() journey block",
        "Agent attempts to run npx playwright test on the specific spec",
        "Agent attempts to run make test-e2e for the full suite after specific spec passes",
        "E2E spec uses getByTestId or getByRole locators, not CSS class selectors"
      ]
    },
    {
      "id": 2,
      "prompt": "Refactor GridTreeBuilder to use a recursive algorithm instead of the iterative approach — no API or UI changes",
      "expected_output": "Agent correctly identifies this as non-UI change and skips E2E test creation",
      "files": [],
      "expectations": [
        "Agent does NOT create or modify any E2E spec file in tests/E2E/specs/",
        "Agent does NOT run npx playwright test or make test-e2e",
        "Agent focuses on unit/integration tests for the refactored code"
      ]
    },
    {
      "id": 3,
      "prompt": "Fix the bug where collapsing a section doesn't persist after page reload",
      "expected_output": "Agent extends existing collapse.spec.ts rather than creating a new spec file, runs the spec",
      "files": [],
      "expectations": [
        "Agent reads existing E2E specs in tests/E2E/specs/ before writing",
        "Agent modifies tests/E2E/specs/collapse.spec.ts rather than creating a new spec file",
        "Extended spec uses test.step() pattern for the persistence verification",
        "Agent attempts to run npx playwright test on collapse.spec.ts",
        "Agent attempts to run make test-e2e for the full suite"
      ]
    }
  ]
}
